using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class josheb : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int a = 10;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
